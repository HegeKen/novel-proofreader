# 可复现依赖解析：新装依赖写精确版本，避免 ^ 范围在他人机器上解析出不同版本。
# 注意：这里刻意不设 frozen-lockfile=true，否则 `pnpm add` 会被自己挡住；
# 发布构建由 scripts/build-release.mjs 显式传入 --frozen-lockfile。
save-exact=true
strict-peer-dependencies=false
